/** Partilhado entre cliente e servidor (não usar `.server` no componente da rota). */
export const DEFAULT_ENIGMA_ENTRANCE_PROMPT =
  'Este enigma está protegido por senha. Digite-a para continuar.'
